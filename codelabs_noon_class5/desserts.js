const desserts = ["ice cream", "brownies", "chocolate chip cookies", "pie"];

const listDessertsElment = document.getElementById("list-desserts");

for(let i = 0; i < desserts.length; i++){
    // step 1: create a list item for the dessert
    const listiItemElement = document.createElement("li");

    // step 2: add content to the element

    // step 3: Append the element to the list of desserts element

}